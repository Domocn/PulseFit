package com.example.pulsefit.data.model

enum class NdProfile(val label: String, val description: String) {
    ADHD("Keep it exciting", "Fast-paced with variety and rewards"),
    ASD("Keep it calm", "Predictable routines with gentle transitions"),
    AUDHD("Mix of both", "Balanced approach with flexibility"),
    STANDARD("Standard", "Default experience for all users")
}
