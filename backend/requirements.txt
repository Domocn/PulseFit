# Production dependencies — direct imports only.
# Transitive deps are resolved by pip; do not paste a `pip freeze` back in here.
# Previous 127-line freeze kept at requirements.railway-freeze.txt for reference.

# --- web framework / server ---
fastapi==0.109.0
uvicorn==0.27.0
uvloop==0.22.1
httptools==0.7.1
pydantic==2.9.2

# --- database ---
motor==3.3.2           # pulls pymongo, which provides `bson`

# --- auth ---
firebase-admin==6.6.0  # imported lazily at server.py:51; see note below

# --- integrations ---
elevenlabs==2.36.0
httpx==0.28.1
cryptography==46.0.4   # AdMob SSV signature verification

# --- config ---
python-dotenv==1.0.0

# NOTE: firebase-admin is the heaviest remaining dependency — it pulls
# google-api-core and grpcio (~200-300 MB installed). It is used only to verify
# Firebase ID tokens. If memory is still high after slimming, that verification
# can be done with PyJWT against Google's public certs and this dep dropped.
